apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ cd_pvc }}"
  namespace: "{{ cd_namespace }}"
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi