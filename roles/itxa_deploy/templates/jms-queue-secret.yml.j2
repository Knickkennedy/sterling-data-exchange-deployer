apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: "{{ jms_secret }}"
  namespace: "{{ b2bi_namespace }}"
data:
  JMS_USERNAME: "{{ jms_user | b64encode }}"
  JMS_PASSWORD: "{{ mq_appuser_password | b64encode }}"
