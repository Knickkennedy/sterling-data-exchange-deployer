apiVersion: v1
kind: Secret
type: kubernetes.io/dockerconfigjson
metadata:
  name: "{{ itxa_registry_secret }}"
  namespace: "{{ itxa_namespace }}"
data:
  .dockerconfigjson: "{{ new_secret | to_json | b64encode }}"
