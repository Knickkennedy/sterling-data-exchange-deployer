apiVersion: v1
kind: Secret
type: kubernetes.io/dockerconfigjson
metadata:
  name: "{{ pem_std_registry_secret }}"
  namespace: "{{ pem_std_namespace }}"
data:
  .dockerconfigjson: "{{ new_secret | to_json | b64encode }}"
