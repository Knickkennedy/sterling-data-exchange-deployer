---
- name: Create root output directory if it does not exist
  ansible.builtin.file:
    path: ../output
    state: directory
    mode: '0755'

- name: Create global output directory if it does not exist
  ansible.builtin.file:
    path: ../output/global
    state: directory
    mode: '0755'
