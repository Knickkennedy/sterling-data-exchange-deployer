apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ seas_pvc }}"
  namespace: "{{ seas_namespace }}"
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 2Gi