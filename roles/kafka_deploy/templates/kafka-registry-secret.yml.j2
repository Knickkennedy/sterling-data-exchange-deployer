apiVersion: v1
kind: Secret
type: kubernetes.io/dockerconfigjson
metadata:
  name: "{{ kafka_registry_secret }}"
  namespace: "{{ kafka_namespace }}"
data:
  .dockerconfigjson: "{{ kafka_new_secret | to_json | b64encode }}"
